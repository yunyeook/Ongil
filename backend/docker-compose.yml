version: "3.8"

services:
  app:
    build:
      context: ./ongil
      dockerfile: Dockerfile
    image: rabent0207/ongil:latest
    env_file:
      - .env
    ports:
      - "8080:8080"
    command: tail -f /dev/null