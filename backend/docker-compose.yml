version: "3.8"

services:
  app:
    build:
      context: ./ongil
      dockerfile: Dockerfile
      target: build
    env_file:
      - .env
    volumes:
      - ./ongil/build:/app/build
    command: tail -f /dev/null