version: "3.8"

services:
  app:
    build:
      context: ./ongil
      dockerfile: Dockerfile
      target: build
    image: rabent0207/ongil:latest
    env_file:
      - .env
    command: tail -f /dev/null