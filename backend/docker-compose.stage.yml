version: "3.8"

services:
  app:
    build:
      context: ./ongil
      dockerfile: Dockerfile
    image: rabent0207/ongil-staging:latest
    env_file:
      - .env
    command: tail -f /dev/null
